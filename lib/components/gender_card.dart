import 'package:flutter/material.dart';


class GenderCard extends StatelessWidget {
  final Widget? child;
  
  const GenderCard({super.key, this.child});


  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}